import SwiftUI

/// Blank welcome screen for new tabs.
struct WelcomeView: View {
    var body: some View {
        VStack {
            Spacer()
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
    }
}
